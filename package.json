{
  "name": "strava-oauth-app",
  "version": "1.0.0",
  "description": "A simple app to interact with Strava OAuth using Netlify",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && cp -R src/* dist/",
    "start": "netlify dev"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {}
}
